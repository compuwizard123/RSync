rsync configuration tool --- a GTK+ tool for configuring and configuring rsync jobs

Dependencies:
 * regex

Development dependencies:
 * pychecker
 * cloc
 * mock (python-mock package)
 * coverage (python-coverage package)
 * cc.py and pygenie.py (bundled in the tools directory, but not under the GPLv3)
